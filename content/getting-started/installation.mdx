---
title: Installation
description: Steps to install and set up Litebase.
section: Getting Started
tags: ["getting-started", "installation"]
---

<Lead>
  Litebase is a single binary that you can download and run on your local
  machine or server. Follow these steps to get started with Litebase.
</Lead>

With the Litebase CLI you can issue authentication credentials, manage databases, execute SQL commands and more. The CLI is available for Linux, macOS, and Windows. You can download the latest version
using the following command in your terminal:

```shell
curl -sSL https://litebase.com/install.sh | bash
```

Or, you can download the latest version from the [Litebase GitHub repository](https://github.com/litebase/litebase/releases).

Once installed, you can run `litebase --help` to see a list of available commands and flags. You can also refer to the [CLI Reference](/docs/cli/reference).
